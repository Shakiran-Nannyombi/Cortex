build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - bash
    - -c
    - cd /app/backend && flask --app wsgi:app db upgrade
run:
  web: bash -c "cd /app/backend && gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT wsgi:app"
